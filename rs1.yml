apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: myweb-rs
spec:
  replicas: 3
  selector:
  template:
    metadata:
      name: myweb-pod
      labels:
        env: production
        region: IN
    spec:
      containers:
      -  name: myweb-con
         image: httpd
